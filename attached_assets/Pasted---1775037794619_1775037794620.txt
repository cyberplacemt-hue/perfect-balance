Работай ТОЛЬКО в существующем проекте сайта. Не создавай новый проект, не меняй стек и архитектуру.

Задача: настроить отправку заказов с сайта через SMTP Mail.ru, используя почтовый ящик podkovamsk@mail.ru. Resend И ДРУГИЕ email‑сервисы НЕ использовать.

Нужно:
1) Найти форму заказа/заявки на сайте.
2) Сделать так, чтобы при отправке формы письмо уходило через SMTP smtp.mail.ru от имени podkovamsk@mail.ru и приходило на указанный адрес (можно To = podkovamsk@mail.ru).
3) Не ломать текущий интерфейс и логику формы.

Используй SMTP Mail.ru со стандартными настройками:
- SMTP host: smtp.mail.ru
- Port: 465
- Security: SSL/TLS включено
- Username: полный адрес почты, то есть podkovamsk@mail.ru
- Password: отдельный пароль приложения для SMTP (НЕ обычный пароль от почты, я задам его в переменных окружения)
См. официальные настройки Mail.ru SMTP: smtp.mail.ru, порт 465, SSL/TLS, требуется авторизация [web:98][web:100].

Требования к реализации:
- Настроить backend‑код (Node.js или тот стек, который уже используется), который:
  - принимает данные формы (имя, телефон, комментарий, выбранные товары и т.п.);
  - через SMTP smtp.mail.ru отправляет письмо на podkovamsk@mail.ru;
  - формирует понятный subject, например: "Новый заказ с сайта";
  - в теле письма перечисляет все поля формы.
- Все чувствительные данные (SMTP логин и пароль приложения Mail.ru) брать из переменных окружения (Replit Secrets), НИЧЕГО не хардкодить в репозитории.
  - Например:
    - MAIL_SMTP_HOST = smtp.mail.ru
    - MAIL_SMTP_PORT = 465
    - MAIL_SMTP_USER = podkovamsk@mail.ru
    - MAIL_SMTP_PASS = <пароль приложения Mail.ru>
- Можно использовать популярную библиотеку для SMTP (например, nodemailer или аналог, подходящий для текущего стека).

UX формы:
- После успешной отправки:
  - очистить поля формы или заблокировать их;
  - показать сообщение на русском: "Спасибо! Ваш заказ отправлен. Мы свяжемся с вами в ближайшее время."
- При ошибке:
  - не падать с необработанной ошибкой;
  - показать пользователю сообщение: "Произошла ошибка при отправке заказа. Попробуйте ещё раз или свяжитесь по email: podkovamsk@mail.ru".

Дополнительно:
- Не менять внешний вид формы радикально.
- Не трогать другие части сайта без необходимости.
- Не использовать Resend, SendGrid и прочие внешние email‑сервисы.

Порядок работы:
1. Определи, в каком файле/роуте сейчас должна обрабатываться форма заказа (или создай отдельный эндпоинт, если его нет).
2. Добавь/настрой отправку письма через SMTP Mail.ru с использованием переменных окружения.
3. Обнови frontend формы так, чтобы она вызывала этот эндпоинт и правильно обрабатывала успех/ошибку.
4. В конце выведи:
   - список изменённых файлов,
   - краткое описание, что именно изменено,
   - какие переменные окружения нужно задать в Replit (имена переменных, без значений).