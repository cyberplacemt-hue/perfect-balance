Работай ТОЛЬКО в существующем проекте по адресу https://asset-manager--cyberplacemt.replit.app/. 
Не создавай новый проект и не меняй архитектуру приложения.

Задача: 
1) Добавить на сайт явно отображаемый email для связи: podkovamsk@mail.ru.
2) Сделать так, чтобы при отправке заказа/заявки с сайта все данные формы приходили на email podkovamsk@mail.ru.
3) Добавить рядом с формой/в блоке контактов дружелюбный текст с email.

ВАЖНО:
- Сначала проанализируй проект: где находится форма заказа/заявки и как сейчас обрабатываются данные (есть ли backend-роут, используется ли fetch/axios, есть ли уже обработчик на сервере).
- Ничего не удаляй и не ломай в текущей логике заказа.
- Не меняй внешний вид формы радикально, только добавь функциональность отправки на почту и текст.
- Если нужно подключить email‑сервис (например, Resend, Mailgun, SendGrid или другой), сделай это стандартным способом для Replit и используй переменные окружения для ключей.
- Все тексты интерфейса должны быть на русском.

СДЕЛАТЬ:

1. ОТОБРАЖЕНИЕ ПОЧТЫ И ТЕКСТА
- Найди подходящее место (футер, блок контактов или рядом с формой заказа).
- Добавь текст (дословно, с сохранением разметки):

  Мы всегда на связи: обсуждаем, советуем, считаем.  
  Пишите на **podkovamsk@mail.ru** — это самый быстрый способ достучаться до нас.

- При реализации на сайте сделай email кликабельной ссылкой mailto:podkovamsk@mail.ru.
- Не ломай адаптивность и общий стиль сайта.

2. ОТПРАВКА ЗАКАЗА НА ПОЧТУ
- Найди форму заказа/заявки (поле имени, телефона, товара и т.п.).
- Если серверной обработки ещё нет — добавь минимальный backend‑роут (например, /api/order или аналогичный для текущего стека), который:
  - принимает данные формы (имя, телефон, комментарий, выбранные товары и т.п.);
  - формирует человекочитаемое письмо;
  - отправляет это письмо на podkovamsk@mail.ru.
- Для отправки email используй один из стандартных вариантов для Replit (например, Resend/Mailgun/SendGrid) — по возможности без лишних зависимостей.
- Все секретные ключи для email‑сервиса вынеси в переменные окружения, не хардкоди ключи в код.
- В теле письма должны быть:
  - дата/время;
  - все поля формы;
  - тема письма вроде "Новый заказ с сайта".

3. UX ФОРМЫ
- После успешной отправки заказа:
  - очистить форму или заблокировать её;
  - показать пользователю сообщение на русском, например: "Спасибо! Ваш заказ отправлен. Мы свяжемся с вами в ближайшее время."
- При ошибке отправки:
  - не падать с ошибкой;
  - показать пользователю сообщение: "Произошла ошибка при отправке заказа. Попробуйте ещё раз или свяжитесь по email: podkovamsk@mail.ru".
- Не менять дизайн формы радикально.

4. БЕЗОПАСНОСТЬ И ВАЛИДАЦИЯ
- Добавь базовую серверную валидацию (проверка обязательных полей).
- Защити от пустых отправок.
- Не добавляй лишний сложный функционал (капча и т.п.) без необходимости.

5. ОТЧЁТ
После внесения изменений:
- Выведи список всех изменённых файлов.
- Кратко опиши, что именно было добавлено (где показан email и текст, где обработчик отправки, какой email‑сервис подключён).
- Укажи, какие переменные окружения нужно добавить в Replit Secrets (например, ключ API для email‑сервиса).